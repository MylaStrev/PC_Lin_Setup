bash or shell script
this is what is used in in the CLI.
commands found in the CLI can be put into a bash script, however specific commands may require dependencies to be installed.
for example, using "flatpak install example" wont work unless flatpak is installed on the system.


